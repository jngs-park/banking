spring:
  datasource:
    url: jdbc:mysql://localhost:3306/basic_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: PW7480
  jpa:
    hibernate:
      ddl-auto: update   # 개발 단계만! (테이블 자동 생성)
    show-sql: true
    properties:
      hibernate.format_sql: true

    redis:
      host: localhost
      port: 6379

    kafka:
      bootstrap-servers: localhost:9092



